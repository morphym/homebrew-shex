# Homebrew shex

Install the native `shex` binary on macOS or Linux without installing Rust:

```sh
brew install morphym/shex/shex
```

The formula selects the matching release for Apple Silicon, Intel macOS,
Linux arm64, or Linux x86-64 and verifies its SHA-256 checksum.
